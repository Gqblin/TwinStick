using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmmoBox : MonoBehaviour
{
    //[SerializeField] Player player;
    //[SerializeField] private int clipsAmount = 3;
    //PlayerEquipment playerEquipment;

    //private float pickUpDistance = 1.5f;

    //void Start()
    //{
    //    player = FindObjectOfType<Player>();
    //    playerEquipment = player.GetComponent<PlayerEquipment>();
    //}

    //void Update()
    //{
    //    float distance = Vector3.Distance(player.transform.position, transform.position);
    //    if (distance < pickUpDistance)
    //    {
    //        if (Input.GetKeyDown(KeyCode.E) || Input.GetKeyDown(KeyCode.F))
    //        {
    //            GameObject weapon = playerEquipment.ReturnWeapon();
    //            if(weapon.GetComponent<BasicWeapon>() != null)
    //            {
    //                BasicWeapon basicWeapon = weapon.GetComponent<BasicWeapon>();
    //                Destroy(gameObject);
    //            }
    //        }
    //    }
    //}
}
